interface:
  display_name: "AI Opportunity Watcher"
  short_description: "Bootstrap and operate the multi-source AI opportunity watcher."
  default_prompt: "Use $ai-opportunity-watcher to bootstrap the watcher into the current workspace and create two Codex automations: watcher at 09:00, 12:00, 15:00, 18:00, and 21:00, and reply processing every hour from 09:00 through 23:00."
policy:
  allow_implicit_invocation: true
